[build]
  command = "npm install && npm run build"
  publish = "."
  functions = "functions"

[functions]
  node_bundler = "nft"
  included_files = []

[[redirects]]
  from = "/api/youtube-upload"
  to = "/.netlify/functions/youtube-upload"
  status = 200
